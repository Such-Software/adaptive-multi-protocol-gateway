[project]
name = "adaptive-multi-protocol-gateway"
version = "0.1.0"
description = "Static-first multi-protocol site compiler and ingress planner."
requires-python = ">=3.11"
license = "Apache-2.0"

[project.scripts]
ampg = "ampg.cli:main"
ampg-dns-manifest = "ampg.dns_manifest_cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
